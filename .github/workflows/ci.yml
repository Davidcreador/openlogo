name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.28.2
          run_install: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run tests
        run: pnpm test

      - name: Typecheck
        run: pnpm typecheck

      - name: Build production app
        run: pnpm build
        env:
          VITE_DESIGN_MATE_SERVICE_URL: /design-mate

      - name: Smoke-test editor and Design Mate
        run: pnpm smoke:editor
        env:
          EXPECT_DESIGN_MATE_REMOTE_CONFIGURED: "true"

      - name: Smoke-test Design Mate service
        run: pnpm --filter @openlogo/design-mate-service smoke
