# Build-time service root. Leave unset for local-only Design Mate.
# HTTPS is required except for loopback development; a same-origin path is
# recommended in production.
VITE_DESIGN_MATE_SERVICE_URL=http://127.0.0.1:8787
