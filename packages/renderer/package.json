{
  "name": "@openlogo/renderer",
  "version": "0.1.0",
  "private": true,
  "description": "CanvasKit (Skia WASM) scene renderer for OpenLogo: camera, drawing, hit-testing.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@openlogo/core": "workspace:*",
    "canvaskit-wasm": "^0.40.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.0.17"
  }
}
